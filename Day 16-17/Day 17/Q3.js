// Define a function called calculateArea that takes the length of a square
//  as input and returns its area.
 

function calculateArea(length) {
    return length * length;
}

var ans = calculateArea(5);

console.log(ans)